# 1. Dependencies
node_modules/
package-lock.json  # Optional: Keep this if you want to lock versions
yarn.lock

# 2. Build Outputs
dist/
build/
coverage/

# 3. Security (CRITICAL)
.env
.env.local
.env.development.local
.env.test.local
.env.production.local
# Ignore the old config if you still have it and it has secrets
server/config/config.json 

# 4. Logs
logs
*.log
npm-debug.log*

# 5. OS & Editor Junk
.DS_Store
Thumbs.db
.vscode/
.idea/
*.swp